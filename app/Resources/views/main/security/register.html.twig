{% extends "main/base.html.twig" %}

{% block content %}
    <div class="box">
        <div class="col-lg-12">
            <hr>
            <h2 class="intro-text text-center"><strong>Register</strong></h2>
            <hr>
        </div>
    <div class="col-lg-6 col-lg-offset-3">
        {% form_theme register_form 'bootstrap_3_layout.html.twig' %}
        {{ form_start(register_form) }}
        {{ form_widget(register_form) }}
        <input type="submit" value="Register!" formnovalidate class="btn btn-primary">
        {{ form_end(register_form) }}
    </div>
    </div>

{% endblock %}