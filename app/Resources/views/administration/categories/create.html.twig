{% extends "administration/base.html.twig" %}

{% block content %}
    <div class="box">
    <div class="col-lg-12">
        <hr>
        <h2 class="intro-text text-center"><strong>Create category</strong></h2>
        <hr>
    </div>
    <div class="col-lg-6 col-lg-offset-3">
        {% form_theme form_create 'bootstrap_3_layout.html.twig' %}
        {{ form_start(form_create) }}
        {{ form_widget(form_create) }}
        <input type="submit" value="Add category!" class="btn btn-default" formnovalidate>
        {{ form_end(form_create) }}
    </div>
    </div>
{% endblock %}