{% extends 'administration/base.html.twig' %}

{% block content %}
    <div class="box">
        <div class="col-lg-12">
            <hr>
            <h2 class="intro-text text-center"><strong>Confirm {{ user.username }} ban!</strong></h2>
            <hr>
        </div>
        <div class="col-lg-6 col-lg-offset-3">
            {{ form_start(ban_form) }}
            <input type="submit" value="Ban user" class="btn btn-danger">
            {{ form_end(ban_form) }}
        </div>
    </div>

{% endblock %}